{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "timer"],
  "permissions": [
    "core:default",
    "opener:default",
    "core:window:default"
  ]
}
